using System;
using System.Collections.Generic;
using System.Text;

namespace ChaimWarehouse.QueryFolder
{
    /// <summary>
    /// Represents a parsed query.
    /// Holds the SELECT, WHERE and ORDER BY parts separately.
    /// </summary>
    public class Query
    {
        public override string ToString()
        {
            return $"";
        }
    }
}
